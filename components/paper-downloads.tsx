import { buttonVariants } from '@/components/ui/button';
export function PaperDownloads({ id, scientificReady = false }: { id: number; scientificReady?: boolean }) {
  return <div className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-xl border border-pink-200 bg-white p-5 text-rose-950"><h3 className="text-lg font-semibold">Scientific Research Paper</h3><p className="mt-2 text-sm leading-6">Evidence findings, methods, independent AI check, limitations, source links and agent attribution.</p>{scientificReady ? <><a href={'/papers/'+id+'/scientific'} className="mt-4 block text-sm text-primary underline">Read full paper</a><a download href={'/api/papers/'+id+'/scientific/pdf'} className={buttonVariants({className:'mt-4 w-full'})}>Download scientific PDF</a></> : <p className="mt-4 rounded-lg border p-3 text-sm">Draft and source check in progress. The PDF appears after verification.</p>}</div><div className="rounded-xl border border-pink-200 bg-white p-5 text-rose-950"><h3 className="text-lg font-semibold">Plain-Language Brief</h3><p className="mt-2 text-sm leading-6">The existing source context explained for general readers.</p><a download href={'/api/papers/'+id+'/pdf?version=layman'} className={buttonVariants({className:'mt-4 w-full'})}>Download plain-language PDF</a></div></div>;
}
