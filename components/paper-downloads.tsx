import { buttonVariants } from '@/components/ui/button';
export function PaperDownloads({ id }: { id: number }) {
  return <div className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-xl border border-pink-200 bg-white p-5 text-rose-950"><h3 className="text-lg font-semibold">Scientific Research Paper</h3><p className="mt-2 text-sm leading-6">Abstract, methods, results, discussion, limitations and references.</p><a download href={'/api/papers/'+id+'/pdf?version=scientific'} className={buttonVariants({className:'mt-4 w-full'})}>Download scientific PDF</a></div><div className="rounded-xl border border-pink-200 bg-white p-5 text-rose-950"><h3 className="text-lg font-semibold">Plain-Language Paper</h3><p className="mt-2 text-sm leading-6">The same evidence explained simply: what it means and what remains unknown.</p><a download href={'/api/papers/'+id+'/pdf?version=layman'} className={buttonVariants({className:'mt-4 w-full'})}>Download plain-language PDF</a></div></div>;
}
